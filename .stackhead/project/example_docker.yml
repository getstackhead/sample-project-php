---
domain: mydomain.com
deployment:
  type: docker
  settings:
    services:
      app:
        image: getstackhead/project-demo-php
        expose_port: 8080
        links:
          - db
      db:
        image: mariadb:10.5
        environment:
          MYSQL_ROOT_PASSWORD: example
